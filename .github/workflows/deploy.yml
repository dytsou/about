name: Deploy to dytsou.github.io

on:
  workflow_run:
    workflows: ["CI"]
    types:
      - completed
    branches: [main]
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - "src/**"
      - ".github/**"
      - "public/**"
      - "package.json"
      - "vite.config.ts"

permissions:
  contents: write

jobs:
  checks:
    # Run checks for push and manual workflow_dispatch (CI already ran for workflow_run)
    if: github.event_name != 'workflow_run'
    runs-on: ubuntu-latest
    steps:
      - name: Checkout source
        uses: actions/checkout@v6

      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version: 20

      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 10

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Run linter
        run: pnpm lint

      - name: Run type check
        run: pnpm typecheck

      - name: Build site
        run: pnpm build

  deploy:
    # Only deploy if:
    # - CI workflow completed successfully (for workflow_run), OR
    # - checks job completed successfully (for push and workflow_dispatch)
    needs: [checks]
    if: |
      always() &&
      (
        (github.event_name == 'workflow_run' && github.event.workflow_run.conclusion == 'success') ||
        (github.event_name != 'workflow_run' && needs.checks.result == 'success')
      )
    runs-on: ubuntu-latest
    steps:
      - name: Checkout source
        uses: actions/checkout@v6
        with:
          ref: ${{ github.event.workflow_run.head_branch || github.ref }}

      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version: 20

      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 10

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Build site
        run: pnpm build

      - name: Deploy to dytsou.github.io
        uses: JamesIves/github-pages-deploy-action@v4.7.4
        with:
          token: ${{ secrets.GH_PAGES_TOKEN }}
          repository-name: dytsou/dytsou.github.io
          branch: main
          folder: dist
          clean: true
          clean-exclude: |
            error.html
            no-ref-page.html
            README.md
            safe-browsing-warning.html
            home-redirect-page.html
            CNAME
          single-commit: true
